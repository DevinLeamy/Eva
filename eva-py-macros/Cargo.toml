[package]
name = "eva-py-macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true
# crate-type = ["cdylib", "rlib"]

[dependencies]
quote = "1.0.33"
syn = { version = "2.0.39", features = ["full"] }
eva = { path = "../eva" }
